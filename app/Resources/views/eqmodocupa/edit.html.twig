{% extends "layout.html.twig" %} 
{% block titulo %} EQUIVALENCIA DE MODOS DE OCUPACIÓN (MODOCUPA) **{{accion}}**
    <a href="{{path("queryEqModOcupa")}}" class="btn btn-success">
         <span class="glyphicon glyphicon-th-list"></span> Volver</a>
{% endblock %}

{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
        {% include 'eqmodocupa/form.html.twig' %}
    </div>
    <div class="clearfix"> </div>
{% endblock %}  