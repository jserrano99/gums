{% extends "layout.html.twig" %} 
{% block titulo %} EQUIVALENCIAS CUENTAS DE COTIZACIÓN (EQ_MOVIPAT) **CONSULTA** 
    <a href="{{path("addEqMoviPat")}}" 
       class="btn btn-success"
       target="_blank">
        <span class="glyphicon glyphicon-new-window"></span> Nueva Equivalencia</a>
    <hr/>
{% endblock %} 

{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
            <hr/>
        {% endfor %}
        {{ sg_datatables_render(datatable) }}
    </div>
{% endblock %}